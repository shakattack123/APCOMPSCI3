using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class hifuhsiufh : MonoBehaviour
{
    public Transform manwell;
    // Start is called before the first frame update
    void Start()
    {
        string yourmom = "my mom";
        Debug.Log("Error");
        //The one below is a loop
        {
            for (int s = 0; s > 100; s++)

                Debug.Log("The loop");
        }
    }
    // Update is called once per frame
    void Update()
    {
        transform.LookAt(manwell);
    }
   
    
    
}
